Example of using MUI in Blitz Basic to create a simple interface including various gadget types.

Code is in AmiBlitz3 format. This will also load into Blitz 2 without problems, but the resident files required will need to be manually added. Save from Blitz 2 as a .bb2 file instead to also save the resident file status.
